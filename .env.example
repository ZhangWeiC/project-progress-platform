# 对外 HTTP 端口。域名正式上线时建议保持 80，并在云上增加 HTTPS。
APP_PORT=80

# 首次生产启动必填，至少 12 位。现有数据库中仍为 123456 的账号也会被自动替换。
INITIAL_USER_PASSWORD=replace-with-a-strong-password

TZ=Asia/Shanghai

# 仅当前后端跨域部署时填写，多个来源使用英文逗号分隔。
CORS_ORIGIN=
